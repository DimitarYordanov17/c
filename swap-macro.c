#include <stdio.h>

#define swap(t, x, y) t temp; temp = x; y = x

/*
void swap(type, int x, int y) {
    int temp;
    temp = x;
    x = y;
    y = temp;
    return
}
*/

int main () {
    swap(double, x, y);
    return 0;
}
